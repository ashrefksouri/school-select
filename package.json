{
  "name": "school-select",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-schools.mjs",
    "build:dry": "node scripts/build-schools.mjs --dry",
    "validate": "node scripts/build-schools.mjs --validate-only",
    "preview": "npx serve public -p 3001",
    "test": "node scripts/test-build.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
